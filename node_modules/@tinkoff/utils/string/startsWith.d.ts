interface StartsWith {
    (a: string, str: string): boolean;
    (a: string): (str: string) => boolean;
}
declare const _default: StartsWith;
/**
 * Checks if a string starts with the provided prefix
 *
 * @param {string} prefix
 * @param {string} str
 * @return {Boolean}
 * @example
 *
 *      startsWith('a', 'abc')                //=> true
 *      startsWith('b', 'abc')                //=> false
 */
export default _default;
