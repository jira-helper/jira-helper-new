import Status from '../constants/status';
import { ContextState } from './Context.h';
export default class Context {
    private state;
    private internalMeta;
    private externalMeta;
    constructor(initialState?: Partial<ContextState>);
    getState(): ContextState;
    setState(stateOrFunc: Partial<ContextState>): void;
    getInternalMeta(metaName?: string): any;
    getExternalMeta(metaName?: string): any;
    updateInternalMeta(metaName: string, value: Record<string, any>): void;
    updateExternalMeta(metaName: string, value: Record<string, any>): void;
    getStatus(): Status;
    getResponse(): import("../types.h").Response;
    getRequest(): import("../types.h").Request;
    private getMeta;
    private extendMeta;
}
