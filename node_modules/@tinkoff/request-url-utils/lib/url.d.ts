import type { Query, QuerySerializer } from './types';
export declare const serializeQuery: QuerySerializer;
export declare const addQuery: (url: string, query: Query, querySerializer?: QuerySerializer) => string;
export declare const normalizeUrl: (url: string) => string;
